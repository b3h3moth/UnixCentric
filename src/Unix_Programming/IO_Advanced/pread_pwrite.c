#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int main(void) {
    char *buf;
    int in_fd, out_fd, nread;
    off_t in_offst, out_offst;
    size_t in_size, out_size;

    return(EXIT_SUCCESS);
}
