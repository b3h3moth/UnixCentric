#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_LINES 1000      /* massimo numero di righe ordinabili */

char *pline[MAX_LINES];     /* puntatori alle righe */

int main(int argc, char *argv[]) {
    int nlines = 0;         /* righe in ingresso lette */
    int num = 0;            /* ordinamento numerico = 1 */
    return(EXIT_SUCCESS);
}
