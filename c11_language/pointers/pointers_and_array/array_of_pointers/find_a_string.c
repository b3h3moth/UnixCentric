#include <stdio.h>
#include <stdlib.h>

int main(void) {
    char const *unixos[] = {
        "OpenBSD",
        "FreeBSD",
        "Debian",
        "Gentoo",
        "Arch",
        "Solaris"
    };


    return(EXIT_SUCCESS);
}
