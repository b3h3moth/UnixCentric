#include <stdio.h>
#include <stdlib.h>

/* Lo scopo del programma e' la definizione ed implementazione di un array
multidimensionale dinamico. */

int main(void) {
    int rows = 2, cols = 5;

    return(EXIT_SUCCESS);
}
