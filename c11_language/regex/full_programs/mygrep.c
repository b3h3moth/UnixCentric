#include <stdio.h>
#include <stdlib.h>

// Function Prototypes
int match(char *regexp, char *text);

int main(void) {
    return(EXIT_SUCCESS);
}

// Determina se una stringa di testo soddisfa una regular expression
int match(char *regexp, char *text) {

