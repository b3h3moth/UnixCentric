#include <stdio.h>
#include <stdlib.h>

/* 
int snprintf(char *str, size_t size, const char *format, ...);

La funzione snprintf() redirige al massimo 'size' byte dell'output della 
printf() nel buffer 'str', 'format' si occupa di gestire il formato - %s, %d, 
%f etc. - e infine '...' sono gli argomenti opzionali. */

int main(void) {
    return(EXIT_SUCCESS);
}
