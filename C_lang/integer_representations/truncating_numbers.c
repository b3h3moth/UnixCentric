#include <stdio.h>
#include <stdlib.h>

int main(void) {
    /* Talvolta e' necessario convertire un tipo di dato in un altro tipo di
    minor grandezza, in questo caso vi e' la possibilita' che taluni bit 
    possano essere troncati */
    return(EXIT_SUCCESS);
}
