#include <stdio.h>
#include <stdlib.h>

typedef unsigned char BYTE;

int main(void) {
    unsigned int addr;
    int i, n;
    BYTE *ptr;

    printf("main() function, address: %#x\n", (unsigned int main());
    return(EXIT_SUCCESS);
}
