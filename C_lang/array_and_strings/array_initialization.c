#include <stdio.h>
#include <stdlib.h>

#define SIZE 10

int main(void) {

    // I primi 5 elelmenti sono inizializzati, il resto sara' posto a 0
    int vector_a[SIZE] = {10, 20, 30, 40, 50};
    return(EXIT_SUCCESS);
}
