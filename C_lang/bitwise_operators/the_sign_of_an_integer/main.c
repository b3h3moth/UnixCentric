#include <stdio.h>
#include <stdlib.h>

/* The purpose of this program is to find the sign of variable 'value', the
result goes into result 'variable'. It will be positive (0) or negative (-1) */

int main(void) {
    int value = 10;
    int result = -(value < 0);




    return(EXIT_SUCCESS);
}
