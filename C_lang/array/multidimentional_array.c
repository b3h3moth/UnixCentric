#include <stdio.h>
#include <stdlib.h>

#define NUM_PAGES 4
#define NUM_ROWS 2
#define NUM_COLS 2

/* Un array con piu' di due dimensioni puo' essere visto come  una sorta di
pila di tabelle una sopra l'altra; la prima dimensione indica la pagina la
seconda e la terza righe e colonne. */

int main(void) {

    // Array multidimensionale, tridimensionale
    int array[NUM_PAGES][NUM_ROWS][NUM_COLS];

    return(EXIT_SUCCESS);
}
