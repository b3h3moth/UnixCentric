#include <stdio.h>
#include <stdlib.h>

/* The program's purpose is to convert an integer number into a floating point
single precision (IEEE 754) number */

int main(void) {
    return(EXIT_SUCCESS);
}
