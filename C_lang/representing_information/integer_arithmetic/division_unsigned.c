#include <stdio.h>
#include <stdlib.h>

int main(void) {
    /* Si sta considerando la divisione di un numero di tio unsigned per una
    potenza del 2, si intende dimostrare che se il left-shift equivale a
    moltiplicare per due, allora il right-shift corrisponde alla divisione
    per due */

    return(EXIT_SUCCESS);
}
