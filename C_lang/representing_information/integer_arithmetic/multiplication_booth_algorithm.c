#include <stdio.h>
#include <stdlib.h>

signed short multiplication_8bit(signed char x, signed char y);

int main(void) {
    return(EXIT_SUCCESS);
}

/* Booth algorithm per la moltiplicazione numeri 
in two's complement con word-size di 8-bit */
signed short multiplication_8bit(signed char x, signed char y);
