#include <stdio.h>
#include <stdlib.h>
#include <errno.h>
#include <string.h>
#include <unistd.h>
#include <signal.h>

/*

HEADER    : <unistd.h>
PROTOTYPE : unsigned int alarm(unsigned int seconds);
            int pause(void);
SEMANTICS : Il segnale alarm();
            il segnale pause().
RETURNS   : 0 in caso di successo, -1 in caso di errore
--------------------------------------------------------------------------------
*/

int main(int argc, char *argv[]) {
    return(EXIT_SUCCESS);
}
