#include <stdio.h>
#include <stdlib.h>

/* L'allocazione della memoria puo' essere gestita anche mediante una mappatura
della memoria con le syscall mmap() e munmap(). */

int main(void) {
    return(EXIT_SUCCESS);
}
