#include <stdio.h>
#include <stdlib.h>

char *find_occurence(char const *source, char const *str);

int main(void) {
    return(EXIT_SUCCESS);
}

char *find_occurence(char const *source, char const *str) {
