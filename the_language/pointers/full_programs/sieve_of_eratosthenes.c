#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define SIZE 100

/* Lo scopo del programma e' la realizzazione dell'algoritmo detto il crivello
di Eratostene con numeri primi. */

int main(void) {
    char sieve[SIZE];
    char *sp;
    int num;

    // setta il crivello di 
    return(EXIT_SUCCESS);
}
