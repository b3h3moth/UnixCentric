#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum { MAX_LINE = 1000 };

char get_line(void);
int compare_strings(const void * str1, const void *str2);

int main(void) {
    return(EXIT_SUCCESS);
}
