#include <stdio.h>
#include <stdlib.h>

/* Lo standard C11 ha inrodotto le 'anonymous structure', mediante le quali e'
lecito definire strutture prive sia di tag sia di variabile struttura, dotate 
del solo specificatore 'struct' */

int main(void) {
    return(EXIT_SUCCESS);
}
