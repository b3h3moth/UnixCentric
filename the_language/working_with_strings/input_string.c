#include <stdio.h>
#include <stdlib.h>

int main(void) {
    char ch, line [100];
    
    printf("Type a string: ");
    while ((ch = getchar()) != '\n') {
        if (i < 99) {

    return(EXIT_SUCCESS);
}
