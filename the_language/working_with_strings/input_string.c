#include <stdio.h>
#include <stdlib.h>

int main(void) {
    char ch, line [100];
    
    printf("Type a string: ");
    while ((ch = getchar()) != '\n') {
        if (i < 99)
            line[i++] = c;
        else {
            printf("maximum string\n");
            exit(EXIT_FAILURE);
        }
    }
    return(EXIT_SUCCESS);
}
