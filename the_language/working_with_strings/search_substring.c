#include <stdio.h>
#include <stdlib.h>

#define MAX_LEN 40

/* Lo scopo del programma e' di verificare il funzionamento della funzione
strstr() */

int main(void) {
    char lyrics[][MAX_LEN]
    return(EXIT_SUCCESS);
}
